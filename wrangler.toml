#:schema node_modules/wrangler/config-schema.json

name = "eplayerx-homepage"
main = "src/worker.ts"
compatibility_date = "2025-04-01"
compatibility_flags = ["nodejs_compat"]

# Keep the default *.workers.dev address alive.
workers_dev = true

# Public routes for this Worker.
routes = [
	{ pattern = "api.chenluo.cyou", custom_domain = true },
	{ pattern = "tmdb.chenluo.cyou", custom_domain = true },
	{ pattern = "eplayerx.com/blocks*", zone_name = "chenluo.cyou" },
]

[placement]
mode = "smart"

[observability]
enabled = true

# D1 database for EplayerX Blocks (community submissions + approved blocks).
# Create once: `wrangler d1 create eplayerx-blocks`, then paste the returned
# database_id below. Apply schema: `wrangler d1 migrations apply eplayerx-blocks`.
[[d1_databases]]
binding = "DB"
database_name = "eplayerx-homepage"
database_id = "7a464001-94aa-4eca-85b5-5a7a9beb16a9"
migrations_dir = "migrations"

# Existing crawler/snapshot bucket (served publicly via assets.eplayerx.com).
# Bound so the admin panel can upload collection child logos directly.
[[r2_buckets]]
binding = "ASSETS"
bucket_name = "crawler"

# Uncomment to enable cron trigger (e.g. daily crawl at 08:00 UTC)
# [triggers]
# crons = ["0 8 * * *"]
